import { CustomNgStyleDirective } from './custom-ng-style.directive';

describe('CustomNgStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomNgStyleDirective();
    expect(directive).toBeTruthy();
  });
});
